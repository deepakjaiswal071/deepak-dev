[pytest]
testpaths = tests
pythonpath = .
; all tests here are plain functions; disable class-based collection so
; pytest doesn't warn about imported domain classes like TestCase/TestCaseCollection
python_classes =
